//! Unit tests for RepositoryService
//!
//! NOTE: These tests need to be refactored to use the GitProvider abstraction
//! instead of direct HTTP mocking. The tests are temporarily disabled until
//! the refactoring is complete.

#[cfg(test)]
mod tests {
    // Tests temporarily disabled - need refactoring to use GitProvider abstraction
    // TODO: Rewrite tests to mock GitProvider trait instead of HTTP endpoints
}
